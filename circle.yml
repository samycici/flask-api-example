machine:
  services:
    - docker
  python:
    version: 2.7.10
  environment:
      PYTHONPATH: /home/ubuntu/flask-api-example

dependencies:
  override:
    - pip install docker-compose==1.3.3

test:
  override:
    - docker-compose up -d && sleep 15
